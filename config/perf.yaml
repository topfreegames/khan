postgres:
  user: "khan_perf"
  dbName: "khan_perf"
  host: "localhost"
  port: 5433
  sslMode: "disable"

jaeger:
  disabled: true
  samplingProbability: 0.001

extensions:
  dogstatsd:
    host: localhost:8125
    prefix: khan.
    tags_prefix: ""
    rate: 1
